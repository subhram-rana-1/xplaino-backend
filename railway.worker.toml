[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "celery -A app.services.celery_app:celery_app worker --queues=pdf_preprocess,default --concurrency=4 --loglevel=info"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
